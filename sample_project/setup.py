#from distutils.core import setup
#import setuptools
from setuptools import setup


setup(
    name='sample_project',
    description='A sample project',
    packages=['sample'],
    version="0.1",
)
